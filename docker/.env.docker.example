# ==============================================================================
# Evolution API MCP Server - Docker Environment Variables
# ==============================================================================
#
# INSTRUÇÕES:
#   1. Copie este arquivo: cp .env.docker.example .env.docker
#   2. Edite .env.docker com suas credenciais reais
#   3. NUNCA commite o arquivo .env.docker (já está no .gitignore)
#
# ==============================================================================

# ==============================================================================
# PostgreSQL Database
# ==============================================================================
POSTGRES_USER=evolution
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=evolution

# ==============================================================================
# Redis Cache
# ==============================================================================
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE

# ==============================================================================
# Evolution API
# ==============================================================================

# Authentication (IMPORTANTE: Mude esta chave!)
EVOLUTION_API_KEY=CHANGE_ME_TO_SECURE_API_KEY
AUTHENTICATION_TYPE=apikey

# Porta externa do Evolution API (padrão: 8080)
EVOLUTION_API_PORT=8080

# ==============================================================================
# MCP HTTP Server
# ==============================================================================

# Instância padrão do WhatsApp
# Você precisará criar esta instância na primeira vez
EVOLUTION_INSTANCE_NAME=default_instance

# Porta externa do MCP HTTP (padrão: 3000)
MCP_HTTP_PORT=3000

# ==============================================================================
# NOTAS DE SEGURANÇA
# ==============================================================================
#
# 🔒 IMPORTANTE:
#
# 1. POSTGRES_PASSWORD: Use senha forte (mínimo 16 caracteres)
# 2. REDIS_PASSWORD: Use senha forte e diferente do PostgreSQL
# 3. EVOLUTION_API_KEY: Use chave aleatória segura (pode gerar com: openssl rand -hex 32)
# 4. NÃO use estas credenciais em produção!
# 5. NÃO commite o arquivo .env.docker no git!
#
# ==============================================================================
# EXEMPLO DE SENHAS SEGURAS (GERAR AS SUAS!):
# ==============================================================================
#
# Gerar senha PostgreSQL:
#   openssl rand -base64 24
#
# Gerar API key:
#   openssl rand -hex 32
#
# Resultado exemplo:
#   POSTGRES_PASSWORD=xK9mP2vL4nR7wQ1sT6yU8oA3bC5dE0fG
#   EVOLUTION_API_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
#
# ==============================================================================
